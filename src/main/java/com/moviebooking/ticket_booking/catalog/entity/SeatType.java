package com.moviebooking.ticket_booking.catalog.entity;

/** Physical seat tier. Pricing per tier is set per-show (Phase 4), not here. */
public enum SeatType {
    REGULAR,
    PREMIUM,
    RECLINER
}
