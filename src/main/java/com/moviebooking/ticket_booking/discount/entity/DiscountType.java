package com.moviebooking.ticket_booking.discount.entity;

/** PROMO = code-entered (in scope). OFFER = auto-applied (out of scope; the model is ready for it). */
public enum DiscountType {
    PROMO,
    OFFER
}
